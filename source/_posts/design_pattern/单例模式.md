---
title: 设计模式之单例模式
published: false
layout: post
date: 2025-12-02 08:40:00
permalink: /design_pattern/singleton_pattern.html
categories: [设计模式]

---



## 概念

单例模式是一种创建型设计模式，它保证一个类只有一个实例，并提供一个全局访问点来获取这个实例。

**核心思想**：

- 私有化构造函数，防止外部直接创建实例
- 在类内部创建唯一实例
- 提供静态方法供外部获取该实例



## 适用场景

**✅ 适用场景：**

1. **需要全局唯一对象**：如配置管理器、线程池、数据库连接池、缓存等
2. **需要控制资源访问**：如文件管理器、日志管理器等
3. **频繁创建销毁的对象**：使用单例可以减少系统开销
4. **共享资源访问**：如计数器、序列生成器等



**🎯 典型例子：**

- 配置管理器（Configuration Manager）
- 日志记录器（Logger）
- 数据库连接池（Database Connection Pool）
- 缓存管理器（Cache Manager）
- 线程池（Thread Pool）
- 任务管理器（Task Manager）



## 类图

![image-20251202084753901](https://raw.githubusercontent.com/duwei0227/picbed/main/blogs/image-20251202084753901.png)

**类图说明：**

- **`Client`（客户端）**：通过 `getInstance()` 方法获取单例实例
- **`Singleton`（单例类）**：
  - `-instance: Singleton` - 静态私有实例
  - `-Singleton()` - 私有构造函数
  - `+getInstance(): Singleton` - 静态公有方法，返回唯一实例
  - `+businessMethod()` - 单例的业务方法



## 实现方式

### 1、饿汉式单例（线程安全）

```java
package cn.probiecoder.singleton;

/**
 * 饿汉式单例
 * 类加载时就创建实例，线程安全
 */
public class EagerSingleton {
    private static final EagerSingleton INSTANCE = new EagerSingleton();

    // 私有构造函数
    private EagerSingleton() {
        System.out.println("饿汉式单例实例被创建");
        // 防止通过反射创建实例
        if (INSTANCE != null) {
            throw new RuntimeException("单例对象已被创建，禁止通过反射创建新实例");
        }
    }

    public static EagerSingleton getInstance() {
        return INSTANCE;
    }

     // 业务方法
    public void showMessage() {
        System.out.println("Hello from EagerSingleton! HashCode: " + this.hashCode());
    }
}

```



### 2、懒汉式单例（线程不安全）

```java
package cn.probiecoder.singleton;

/**
 * 懒汉式单例（基础版 - 线程不安全）
 * 第一次调用getInstance时才创建实例
 */
public class LazySingleton {
    private static LazySingleton instance;
    
    private LazySingleton() {
        System.out.println("懒汉式单例实例被创建");
    }
    
    // 线程不安全版本
    public static LazySingleton getInstance() {
        if (instance == null) {
            instance = new LazySingleton();
        }
        return instance;
    }
    
    public void showMessage() {
        System.out.println("Hello from LazySingleton! HashCode: " + this.hashCode());
    }
}
```





### 3、线程安全的懒汉式单例

```java
package cn.probiecoder.singleton;

/**
 * 线程安全的懒汉式单例
 * 使用synchronized保证线程安全，但性能较差
 */
public class ThreadSafeLazySingleton {
    private static ThreadSafeLazySingleton instance;
    
    private ThreadSafeLazySingleton() {
        System.out.println("线程安全的懒汉式单例实例被创建");
    }
    
    // 使用synchronized保证线程安全
    public static synchronized ThreadSafeLazySingleton getInstance() {
        if (instance == null) {
            instance = new ThreadSafeLazySingleton();
        }
        return instance;
    }
    
    public void showMessage() {
        System.out.println("Hello from ThreadSafeLazySingleton! HashCode: " + this.hashCode());
    }
}
```





### 4、双重检查锁定（Double-Checked Locking）

```java
package cn.probiecoder.singleton;

/**
 * 双重检查锁定单例
 * 既保证线程安全，又提高性能
 */
public class DoubleCheckedSingleton {
    // volatile 确保 instance 变量的可见性和有序性
    private static volatile DoubleCheckedSingleton instance;
    
    private DoubleCheckedSingleton() {
        System.out.println("双重检查锁定单例实例被创建");
    }
    
    public static DoubleCheckedSingleton getInstance() {
        // 第一次检查，避免不必要的同步
        if (instance == null) {
            // 同步代码块, 多个线程可能都会进入这里
            synchronized (DoubleCheckedSingleton.class) {
                // 第二次检查，确保只有一个线程创建实例
                if (instance == null) {
                    instance = new DoubleCheckedSingleton();
                }
            }
        }
        return instance;
    }
    
    public void showMessage() {
        System.out.println("Hello from DoubleCheckedSingleton! HashCode: " + this.hashCode());
    }
}

```





### 5、静态内部类单例（推荐）

```java
package cn.probiecoder.singleton;

/**
 * 静态内部类单例（推荐使用）
 * 既实现懒加载，又保证线程安全，且没有性能损失
 */
public class StaticInnerClassSingleton {
    
    private StaticInnerClassSingleton() {
        System.out.println("静态内部类单例实例被创建");
    }
    
    // 静态内部类
    private static class SingletonHolder {
        private static final StaticInnerClassSingleton INSTANCE = new StaticInnerClassSingleton();
    }
    
    public static StaticInnerClassSingleton getInstance() {
        return SingletonHolder.INSTANCE;
    }
    
    public void showMessage() {
        System.out.println("Hello from StaticInnerClassSingleton! HashCode: " + this.hashCode());
    }
}

```



### 6、枚举单例（最佳实践）

```java
package cn.probiecoder.singleton;

/**
 * 枚举单例（最佳实践）
 * 防止反射攻击，自动序列化机制，线程安全
 */
public enum EnumSingleton {
    INSTANCE;
    
    // 可以添加实例变量和方法
    private String data;
    
    EnumSingleton() {
        System.out.println("枚举单例实例被创建");
        this.data = "Initial Data";
    }
    
    public void showMessage() {
        System.out.println("Hello from EnumSingleton! HashCode: " + this.hashCode());
    }
    
    public String getData() {
        return data;
    }
    
    public void setData(String data) {
        this.data = data;
    }
    
    // 业务方法
    public void businessMethod() {
        System.out.println("执行业务逻辑，当前数据: " + data);
    }
}
```



## 示例

### 1、实际应用 - 配置管理器

```java
package cn.probiecoder.singleton;

import java.util.Properties;

/**
 * 实际应用：配置管理器单例
 */
public class ConfigurationManager {
    private static volatile ConfigurationManager instance;
    private Properties properties;
    
    private ConfigurationManager() {
        System.out.println("配置管理器初始化");
        loadConfiguration();
    }
    
    public static ConfigurationManager getInstance() {
        if (instance == null) {
            synchronized (ConfigurationManager.class) {
                if (instance == null) {
                    instance = new ConfigurationManager();
                }
            }
        }
        return instance;
    }
    
    private void loadConfiguration() {
        properties = new Properties();
        try {
            // 从配置文件加载配置
            properties.load(getClass().getClassLoader().getResourceAsStream("config.properties"));
            System.out.println("配置文件加载成功");
        } catch (Exception e) {
            System.out.println("加载配置文件失败，使用默认配置");
            // 设置默认配置
            properties.setProperty("database.url", "jdbc:mysql://localhost:3306/mydb");
            properties.setProperty("database.username", "admin");
            properties.setProperty("database.password", "password");
            properties.setProperty("app.name", "My Application");
        }
    }
    
    // 获取配置值
    public String getProperty(String key) {
        return properties.getProperty(key);
    }
    
    public String getProperty(String key, String defaultValue) {
        return properties.getProperty(key, defaultValue);
    }
    
    // 设置配置值
    public void setProperty(String key, String value) {
        properties.setProperty(key, value);
    }
    
    // 显示所有配置
    public void displayConfig() {
        System.out.println("=== 当前配置 ===");
        properties.forEach((key, value) -> 
            System.out.println(key + " = " + value)
        );
    }
}
```



### 2、实际应用 - 日志管理器

```java
package cn.probiecoder.singleton;

/**
 * 使用静态内部类实现的单例日志管理器
 * 优点：线程安全、懒加载、无性能损失
 */
public class Logger {
    
    // 日志级别枚举
    public enum LogLevel {
        DEBUG, INFO, WARN, ERROR
    }
    
    // 私有构造函数
    private Logger() {
        this.logLevel = LogLevel.INFO; // 默认日志级别
        System.out.println("Logger初始化，默认日志级别: " + logLevel);
    }
    
    // 静态内部类 - 持有单例实例
    private static class LoggerHolder {
        private static final Logger INSTANCE = new Logger();
    }
    
    // 全局访问点
    public static Logger getInstance() {
        return LoggerHolder.INSTANCE;
    }
    
    // 实例变量
    private LogLevel logLevel;
    
    // 设置日志级别
    public void setLogLevel(LogLevel level) {
        this.logLevel = level;
        System.out.println("日志级别设置为: " + level);
    }
    
    // 获取当前日志级别
    public LogLevel getLogLevel() {
        return logLevel;
    }
    
    // 日志方法
    public void debug(String message) {
        if (shouldLog(LogLevel.DEBUG)) {
            log("DEBUG", message, null);
        }
    }
    
    public void debug(String message, Throwable throwable) {
        if (shouldLog(LogLevel.DEBUG)) {
            log("DEBUG", message, throwable);
        }
    }
    
    public void info(String message) {
        if (shouldLog(LogLevel.INFO)) {
            log("INFO", message, null);
        }
    }
    
    public void info(String message, Throwable throwable) {
        if (shouldLog(LogLevel.INFO)) {
            log("INFO", message, throwable);
        }
    }
    
    public void warn(String message) {
        if (shouldLog(LogLevel.WARN)) {
            log("WARN", message, null);
        }
    }
    
    public void warn(String message, Throwable throwable) {
        if (shouldLog(LogLevel.WARN)) {
            log("WARN", message, throwable);
        }
    }
    
    public void error(String message) {
        if (shouldLog(LogLevel.ERROR)) {
            log("ERROR", message, null);
        }
    }
    
    public void error(String message, Throwable throwable) {
        if (shouldLog(LogLevel.ERROR)) {
            log("ERROR", message, throwable);
        }
    }
    
    // 判断是否需要记录日志
    private boolean shouldLog(LogLevel level) {
        return level.ordinal() >= this.logLevel.ordinal();
    }
    
    // 统一的日志记录方法
    private void log(String level, String message, Throwable throwable) {
        String timestamp = java.time.LocalDateTime.now()
                .format(java.time.format.DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss.SSS"));
        String threadName = Thread.currentThread().getName();
        String className = getCallerClassName();
        
        // 格式化输出
        StringBuilder logMessage = new StringBuilder();
        logMessage.append(String.format("[%s] [%s] [%s] [%s] %s", 
            timestamp, threadName, level, className, message));
        
        // 如果有异常，添加异常信息
        if (throwable != null) {
            logMessage.append("\n").append(getStackTrace(throwable));
        }
        
        // 输出到控制台（实际项目中可能输出到文件或其他地方）
        System.out.println(logMessage.toString());
    }
    
    // 获取调用者类名
    private String getCallerClassName() {
        StackTraceElement[] stackTrace = Thread.currentThread().getStackTrace();
        // 跳过内部调用栈：0-getStackTrace, 1-getCallerClassName, 2-log, 3-debug/info等, 4-实际调用者
        for (int i = 4; i < stackTrace.length; i++) {
            String className = stackTrace[i].getClassName();
            // 跳过Logger类本身
            if (!className.equals(Logger.class.getName())) {
                // 简化类名（去掉包名）
                return className.substring(className.lastIndexOf('.') + 1);
            }
        }
        return "Unknown";
    }
    
    // 获取异常的堆栈信息
    private String getStackTrace(Throwable throwable) {
        StringWriter sw = new StringWriter();
        PrintWriter pw = new PrintWriter(sw);
        throwable.printStackTrace(pw);
        return sw.toString();
    }
    
    // 添加日志到文件的功能
    public void setLogFile(String filePath) {
        // 实际项目中可以实现文件日志功能
        System.out.println("设置日志文件路径: " + filePath);
    }
    
    // 关闭日志资源（如果需要）
    public void shutdown() {
        System.out.println("Logger关闭");
    }
}
```



### Client

```java
package cn.probiecoder.singleton;

/**
 * 客户端测试
 */
public class Client {
    public static void main(String[] args) {
        System.out.println("=== 单例模式测试 ===");
        
        // 测试饿汉式单例
        System.out.println("\n1. 测试饿汉式单例:");
        EagerSingleton eager1 = EagerSingleton.getInstance();
        EagerSingleton eager2 = EagerSingleton.getInstance();
        eager1.showMessage();
        eager2.showMessage();
        System.out.println("是否是同一个实例: " + (eager1 == eager2));
        
        // 测试静态内部类单例
        System.out.println("\n2. 测试静态内部类单例:");
        StaticInnerClassSingleton inner1 = StaticInnerClassSingleton.getInstance();
        StaticInnerClassSingleton inner2 = StaticInnerClassSingleton.getInstance();
        inner1.showMessage();
        inner2.showMessage();
        System.out.println("是否是同一个实例: " + (inner1 == inner2));
        
        // 测试枚举单例
        System.out.println("\n3. 测试枚举单例:");
        EnumSingleton enum1 = EnumSingleton.INSTANCE;
        EnumSingleton enum2 = EnumSingleton.INSTANCE;
        enum1.showMessage();
        enum2.showMessage();
        enum1.businessMethod();
        System.out.println("是否是同一个实例: " + (enum1 == enum2));
        
        // 测试配置管理器
        System.out.println("\n4. 测试配置管理器:");
        ConfigurationManager config1 = ConfigurationManager.getInstance();
        ConfigurationManager config2 = ConfigurationManager.getInstance();
        System.out.println("数据库URL: " + config1.getProperty("database.url"));
        config1.displayConfig();
        System.out.println("是否是同一个实例: " + (config1 == config2));
        
        // 测试日志管理器
        System.out.println("\n5. 测试日志管理器:");
        Logger logger1 = Logger.getInstance();
        Logger logger2 = Logger.getInstance();
        
        logger1.debug("这是一条调试信息");
        logger1.info("这是一条普通信息");
        logger1.warn("这是一条警告信息");
        logger1.error("这是一条错误信息");
        
        // 修改日志级别
        logger1.setLogLevel(Logger.LogLevel.DEBUG);
        logger1.debug("现在可以看到调试信息了");
        
        System.out.println("是否是同一个实例: " + (logger1 == logger2));
        
        // 多线程测试
        System.out.println("\n6. 多线程环境测试:");
        testSingletonInMultiThread();
    }
    
    private static void testSingletonInMultiThread() {
        int threadCount = 5;
        Thread[] threads = new Thread[threadCount];
        
        for (int i = 0; i < threadCount; i++) {
            threads[i] = new Thread(() -> {
                DoubleCheckedSingleton singleton = DoubleCheckedSingleton.getInstance();
                System.out.println(Thread.currentThread().getName() + "获取单例，HashCode: " + singleton.hashCode());
            }, "Thread-" + i);
        }
        
        for (Thread thread : threads) {
            thread.start();
        }
        
        for (Thread thread : threads) {
            try {
                thread.join();
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
    }
}
```







## 六种实现方式对比表格

| 实现方式        | 线程安全 | 懒加载 | 性能  | 防止反射 | 防止序列化 | 代码复杂度 | 推荐指数 |
| :-------------- | :------- | :----- | :---- | :------- | :--------- | :--------- | :------- |
| **饿汉式**      | ✅        | ❌      | ⭐⭐⭐⭐⭐ | ❌        | ❌          | ⭐          | ⭐⭐⭐      |
| **懒汉式-基础** | ❌        | ✅      | ⭐⭐⭐⭐  | ❌        | ❌          | ⭐          | ⭐        |
| **懒汉式-同步** | ✅        | ✅      | ⭐⭐    | ❌        | ❌          | ⭐⭐         | ⭐⭐       |
| **双重检查**    | ✅        | ✅      | ⭐⭐⭐⭐  | ❌        | ❌          | ⭐⭐⭐        | ⭐⭐⭐⭐     |
| **静态内部类**  | ✅        | ✅      | ⭐⭐⭐⭐⭐ | ❌        | ❌          | ⭐⭐⭐        | ⭐⭐⭐⭐⭐    |
| **枚举**        | ✅        | ❌      | ⭐⭐⭐⭐⭐ | ✅        | ✅          | ⭐⭐         | ⭐⭐⭐⭐⭐    |

------